# ===== Admin env (cool-bath) =====
# Copy → .env.local và điền giá trị thật. .env* KHÔNG commit (xem .gitignore).

# Base URL của backend NestJS (apps/backend). Server-only - không lộ ra browser JS.
API_BASE_URL=http://localhost:3000
