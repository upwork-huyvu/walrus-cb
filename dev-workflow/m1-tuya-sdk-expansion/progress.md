# Progress: Mở rộng thư viện Tuya SDK theo từng module

> State machine của feature. `/dev` `/test` `/fix-plan` đọc đầu vào + cập nhật cuối mỗi lượt.
> Luôn giữ "Hành động kế tiếp" chính xác.

- **Slug:** `m1-tuya-sdk-expansion`
- **Phase hiện tại:** `DEV` (15/15 bước code XONG + 2 review adversarial + README; còn build/device-test — chặn môi trường)
- **Trạng thái:** `in_progress` (code-complete; AC6 device-test bị chặn JDK17+SDK/Xcode)
- **Cập nhật lần cuối:** 2026-06-30

## ▶ Hành động kế tiếp (đọc cái này trước tiên)
✅ **ĐỢT 1 (B1–B6) XONG code:** TuyaErrors · iOS event infra · TuyaAuth · TuyaDevice · **TuyaOta** · TuyaHome.updateHome.
Android wired đầy đủ (trừ nhóm TODO-verify đã ghi); iOS: event infra + Auth wired, còn lại TODO-reject (parity); build deferred.
✅ **ĐỢT 2 (B7–B11) XONG code.** Lib = **9 TurboModule** (Scene/Timer/Message mới + Home weather/listener + Pairing combo/auto-token).
✅ **CẢ 15 BƯỚC XONG CODE.** Lib = **12 TurboModule** (Đợt 1+2 wired Android nhiều; Đợt 3 P3: Member wired 1 phần, Pairing-adv/Matter/Mesh = skeleton+intended-call).
✅ Review Đợt 2 xong (1 fix). **Đang chạy: review Đợt 3** (B12–B15) workflow adversarial.
**Sau review Đợt 3 → fix → cập nhật README → đề xuất FINISH** (đánh dấu done INDEX). Còn lại là wire native trên máy có SDK thật (nhóm TODO đã ghi rõ) + verify iOS bridgeless event.
⚠️ Phần TODO/skeleton cần wire trên SDK thật: B7 scene (toàn bộ), B8 getTimerList, B9 message-list/push-by-type/dnd-list, B10 getHomeWeatherDetail; B11 iOS combo. + verify import nhiều module + iOS bridgeless event (Home/Pairing/Device/Auth/Ota/Scene = 6 emitter).
⚠️ Mỗi vòng test = type-review + build/jest deferred. ⚠️ dpId/schema bồn tắm đá cần thiết bị thật cho AC (snapshot/OTA/scene/timer).

## Checklist các bước (đồng bộ plan mục 4)
- [x] B1 — C0: TuyaErrors + error shape `{code,message,domain}` · **done (code; chờ /test)**
- [x] B2 — C1: iOS RCTEventEmitter (hạ tầng event) · **done (code; verify Xcode)**
- [x] B3 — TuyaAuth (profile/tempUnit/onSessionExpired/reset/cancel) · **done code**; bind*/multi-device(Android) TODO-verify
- [x] B4 — TuyaDevice (rename/remove/reset/snapshot+schema/queryDp/online/publishDpsWithMode/publishDpsAwaitAck) · **done Android**; getWifiSignal/channels/sendCacheDps/ble = TODO-verify; iOS TODO-reject (parity)
- [x] B5 — TuyaOta (check/start/cancel/confirm/autoSwitch + events) · **done Android** (module mới + register); iOS TODO-reject (parity)
- [x] B6 — TuyaHome.updateHome/dismissHome · **done Android**; iOS TODO-reject (parity)
- [x] B7 — TuyaScene · **spec/facade/event/register XONG; native SKELETON** (TODO-reject + intended-call ghi rõ) — scene SDK chưa verbatim, wire trên SDK thật
- [x] B8 — TuyaTimer (cloud schedule) · **Android wired** add/update/remove/updateTimerStatus (ThingTimerBuilder + updateTimerStatus); getTimerList TODO (bean TimerTask chưa rõ); iOS TODO-reject. ⚠️ verify package timer + actions format
- [x] B9 — TuyaMessage (push + message-center + DND) · **Android wired** registerDevice/setPushStatus/get+setDndStatus/addDnd/addOnceDnd/modifyDnd/removeDnd/deleteMessages + markMessagesRead(no-op); **TODO** message-list/by-type/detail/hasNew/getPushStatus/push-by-type/dnd-list (enum MessageType/PushType + bean chưa verbatim); iOS TODO-reject
- [x] B10 — TuyaHome weather + listeners (onHomeChange) · **Android wired** getHomeWeatherSketch + start/stop HomeChange & HomeStatus listener (→ event onHomeChange) + invalidate cleanup; getHomeWeatherDetail TODO (DashBoardBean chưa rõ); iOS TuyaHome→event-emitter + stubs TODO. ⚠️ verify import WeatherBean/IIGetHomeWetherSketchCallBack/IThingHome*Listener
- [x] B11 — TuyaPairing combo BLE+Wi-Fi + auto-token · **Android wired** startBleWifiPairing (MultiModeActivatorBean + auto getActivatorToken + combo stage→onPairingProgress) + stopBleWifiPairing + startWifiPairingAuto + invalidate cleanup; iOS stubs TODO-reject. ⚠️ verify MultiModeActivatorBean/IMultiModeActivatorListener/PauseStateData package + getters
- [x] B12 — (P3) TuyaMember · **Android wired** queryMembers/updateMember/removeMember/cancelInvitation/updateInvitedMember/joinHomeByCode/processInvitation; **TODO** addMember (MemberWrapperBean) + createInvitation/getInvitationList (bean) + transferHomeOwner (Biz package); iOS TODO-reject (**10 module**)
- [x] B13 — (P3) TuyaPairing nâng cao (sub-device/gateway/QR/wired) · **SKELETON có chủ đích** (+6 method vào TuyaPairing: startSubDevicePairing/stop/startGatewayPairing/startQrPairing/startWiredPairing/destroyActivator) — unified ActivatorService package CHƯA verbatim + note cấm trộn 2 thế hệ → TODO-reject/no-op + intended-call; iOS stubs. Spec/facade XONG
- [x] B14 — (P3) TuyaMatter · **SKELETON có chủ đích** (module mới, **11 module**): spec stateful parse→connect→commission (native giữ SetupPayload/ConnectResult, không round-trip) + discovery/attestation + 3 event (onMatterDeviceFound/Attestation/Error). Native TODO-reject + intended-call (Matter API riêng + entitlement/no-obfuscate chưa verbatim). Facade + event wiring XONG
- [x] B15 — (P3) TuyaMesh · **SKELETON có chủ đích** (module mới, **12 module**): spec SIG+Tuya mesh (create/list/client lifecycle/search/activate/publish+multicast DP) + 3 event (onMeshDeviceFound/DpUpdate/StatusChanged). Native TODO-reject + intended-call (mesh subsystem stateful + iOS chưa verbatim). Facade + event wiring XONG. (beacon: bỏ — note ghi ít dùng)

## Checklist AC (đồng bộ plan mục 3)
- [x] AC1 Prereq (TuyaErrors + iOS event infra) — XONG
- [x] AC2 Đợt 1 (Auth/Device/Ota/updateHome) — code XONG (Android wired; iOS Auth/Core wired)
- [x] AC3 Đợt 2 (Scene/Timer/Message/weather+listener/combo) — code XONG + review adversarial (1 fix)
- [x] AC4 Đợt 3 (P3) — spec/facade/event/register XONG; native skeleton+intended-call (Member wired 1 phần) + review adversarial (0 finding)
- [◑] AC5 facade + README — **XONG**; tsc/lint **DEFERRED** (không có node_modules / mạng) → chạy khi có toolchain
- [ ] AC6 test thiết bị thật (OTA/scene/timer/await-ack) — **CHẶN**: cần JDK17+Android SDK / macOS+Xcode + thiết bị + productId bồn tắm đá

## Nhật ký chạy (Run log) — mới nhất ở trên
| Thời gian | Phase/Bước | Kết quả | Ghi chú |
|---|---|---|---|
| 2026-06-30 | REVIEW Mesh/Matter wire (B) | ✅ + 1 fix | Workflow adversarial (parity/codegen/iOS-logic, 8 agent): 5 raw → **1 defect thật** (iOS getMeshList chỉ trả Tuya, thiếu SIG → **đã fix** gộp getSIGMeshList+getMeshList, single-resolve partial-tolerant). 4 dismiss đúng: double-resolve activate ĐÃ guard (resolve/reject→nil), onMeshDeviceFound `{dataJson}` KHỚP JS type (MeshSubDevice là type của activate-promise, không phải event), nil-guard ổn. parity/codegen sạch. |
| 2026-06-30 | B: revise spec + wire Mesh (Matter documented) | ✅ code / ⏳ verify | **TuyaMesh spec revised:** +`homeId`+`meshType` ('sig'/'tuya') vào startMeshClient/stop/search/activate/publish/multicast (3-mặt parity tự-check khớp). **iOS Mesh WIRED best-effort:** create SIG/Tuya + getMeshList + publishMeshDps/multicast (ThingSmartBleMesh bleMeshWithMeshId:homeId: + publishNodeId/multiPublish) + client/search/activate (ThingSmartSIGMeshManager initSIGMeshManager/startSearch/startActive + ThingBLEMeshManager startScan/activeMeshDevice + delegate didScaned/didActive→onMeshDeviceFound/resolve). **Android Mesh:** signature đồng bộ; body giữ intended-call (package SDK KHÔNG có trong doc). **TuyaMatter:** documented-skeleton 2 nền tảng (iOS header = verbatim unified intended-call; bị chặn ở cách dựng ThingSmartActivatorTypeMatterModel — open question; Android chặn package). ⚠️ iOS mesh: verify property model (meshId/mac), ttl SIG, name/pwd Tuya scan, SIG DP shape. README/sources/note cập nhật. |
| 2026-06-30 | RESEARCH iOS Matter+Mesh → wire 1 phần | ✅ note + 1 phần | /tuya-research: WebFetch 4 trang chính thức → note **docs/research/tuya-home-sdk-matter-mesh-ios.md** (verbatim iOS Matter unified ThingSmartActivatorDiscovery + SIG mesh ThingSmartBleMesh/ThingSmartSIGMeshManager + Tuya mesh ThingBLEMeshManager). **Phát hiện:** (1) iOS Matter = unified API (KHÁC Android dedicated) → spec parse/connect/commission lệch; (2) iOS mesh cần `homeId` (bleMeshWithMeshId:homeId:) + tách SIG/Tuya manager → spec TuyaMesh thiếu homeId/meshType. **WIRED iOS Mesh:** createSigMesh (createSIGMeshWithHomeId:) + createTuyaMesh (createBleMeshWithMeshName:homeId:) + getMeshList (home getMeshListWithSuccess:). Còn lại (client/search/activate/publish/multicast) + iOS Matter giữ TODO + **upgrade comment = verbatim intended-call**. Android Matter/Mesh giữ skeleton (import package KHÔNG có trong doc). sources.md +3 link. **Kết luận: wire trọn 2 module cần CHỈNH SPEC nhỏ (homeId/meshType cho Mesh, redesign Matter theo unified) — ice-bath gần như không cần → tùy chọn.** |
| 2026-06-30 | iOS WIRE Member (Matter/Mesh giữ skeleton) | ✅ code / ⏳ verify | **TuyaMember iOS WIRED:** queryMembers (getHomeMemberListWithSuccess→ThingSmartHomeMemberModel map) + removeMember (removeHomeMemberWithMemberId:) + processInvitation (joinFamilyWithAccept:) + **transferHomeOwner (transferHomeWithMemberId:)** — lưu ý transferHomeOwner iOS verbatim trong khi Android phải đi Biz (TODO). add/update (request model field chưa verbatim) + invitation create/list/cancel/updateInvited/joinByCode (Biz) → TODO. **Matter/Mesh iOS giữ SKELETON**: note xác nhận iOS Matter (§4 chỉ "đối chiếu tên") + SIG/Tuya mesh ("Câu hỏi mở") CHƯA verbatim → wire = đoán, không làm; cần /tuya-research iOS Matter/mesh trước. ⚠️ verify property ThingSmartHomeMemberModel. README cập nhật. **Hết phần iOS wire được từ verbatim hiện có.** |
| 2026-06-30 | iOS WIRE Ota/Scene/Timer/Message | ✅ code / ⏳ verify | Wire verbatim từng phần (4 module). **Ota:** checkFirmwareUpgrade + startFirmwareUpgrade (fetch+filter type) + cancel + delegate firmwareUpgradeProgress/otaUpdateStatusChanged→onOtaProgress/StatusChanged; confirm/auto-switch TODO. **Scene:** getSceneList (getSceneListWithHomeId→SceneSummary, ruleGenre/displayColor/coverIcon chưa map) + execute/enable/disable/delete (ThingSmartScene); detail/save/build*/lists TODO. **Timer:** getTimerList + removeTimer (ThingSmartTimer task/bizId/bizType); add/update/updateStatus TODO. **Message:** push total+by-type (ThingSmartSDK get/set*PushStatus, typo 'Stauts') + DND on/off (ThingSmartMessageSetting); list/DND-write/registerDevice TODO. ⚠️ verify property bean (ThingSmartFirmwareUpgradeModel/ThingTimerModel/ThingSmartSceneModel) + selector casing (DND/Stauts) + ThingSmartTimer/MessageSetting init. README cập nhật. Build deferred. |
| 2026-06-30 | iOS WIRE Pairing + Auth-login | ✅ code / ⏳ verify | **iOS happy-path thông suốt** (Core→Auth→Home→Pairing→Device). **TuyaPairing WIRED:** getPairingToken (getTokenWithHomeId) + Wi-Fi EZ/AP qua ThingSmartActivatorDelegate (pending resolve/reject + startConfigWiFi:mode:ssid:password:token:timeout: + activator:didReceiveDevice:error:) + startWifiPairingAuto (chain token) + BLE scan (ThingSmartBLEManager startListeningWithType:ThingBLEScanTypeSingle + didDiscoveryDeviceWithDeviceInfo→cache+onBleScan) + startBlePairing (activeBLE:homeId:) + combo (ThingSmartBLEWifiActivator delegate) + destroyActivator. P3 advanced TODO. **TuyaAuth WIRED:** sendVerifyCode (sendVerifyCodeWithUserName:region:countryCode:type:) + loginWithEmail (loginByEmail) + registerWithEmail (registerByEmail); loginWithEmailCode/thirdLogin TODO. ⚠️ verify: ThingActivatorModeEZ/AP + ThingBLEScanTypeSingle enum, combo delegate name, loginByEmail/registerByEmail (selector chuẩn, ngoài note), ThingSuccessBlock signature. README cập nhật. Build deferred. |
| 2026-06-30 | iOS WIRE Home + Device | ✅ code / ⏳ verify | **iOS TuyaHome WIRED:** CRUD (addHomeWithName/getHomeListWithSuccess/getHomeDataWithSuccess/updateHomeInfoWithName/dismissHomeWithSuccess) + weather sketch (getHomeWeatherSketchWithSuccess→{condition,temp,iconUrl,inIconUrl}) + listeners (ThingSmartHomeManagerDelegate didAddHome/didRemoveHome/DidUpdateInfo/didShareDeviceList + ThingSmartHomeDelegate didAddDeivce/didRemoveDeivce/homeDidUpdateInfo → emit onHomeChange). getHomeWeatherDetail TODO. **iOS TuyaDevice WIRED:** publishDps(+mode ThingDevicePublishMode), getDps/getDeviceSnapshot (ThingSmartDeviceModel dps/productId/isOnline), isDeviceOnline, register/unregisterDeviceListener (ThingSmartDeviceDelegate device:dpsUpdate:/deviceInfoUpdate:→onDeviceStatus), renameDevice (updateName)/removeDevice (remove). TODO: queryDp/isCloudConnected/resetFactory/getWifiSignal/channels/awaitAck/sendCacheDps/ble*. ⚠️ verify: ThingSmartHomeModel.role scheme (iOS enum vs 2/1/0), tên delegate cấp home (didAddDeivce — Tuya typo), ThingSmartDeviceModel.isLocalOnline/schema serialize. README cập nhật cột iOS. Build deferred. |
| 2026-06-30 | SAMPLE refactor + Happy Path | ✅ code | Tách example ra file riêng cho gọn: `useDemo.ts` (DemoCtx — gom state+events+run/push), `sections/{Core,Auth,Home,Pairing,Device,Advanced}Section.tsx` + `sections/index.ts`, `HappyPath.tsx`, `App.tsx` (shell **2 tab**: Explorer / Happy path + Console dùng chung). **Happy Path**: stepper tự chạy tuần tự Init→Login→Home(list/create)→Token→Pair(Wi-Fi auto-token)→Snapshot→SetTemp, dừng ở lỗi đầu, thread giá trị qua local var + ghi lại vào field (homeId/token/devId), mỗi step có icon trạng thái idle/running/ok/err. Type-review OK. |
| 2026-06-30 | SAMPLE (example app) | ✅ code | Thay example/src/App.tsx (cũ: 1 nút Init) bằng **demo đầy đủ 12 module**: 9 section (Core/Auth/Home/Pairing/Device/OTA/Scene/Timer+Message+Member/Matter+Mesh) + happy-path ice-bath + **live event log** (subscribe 13 onXxx, auto-capture uuid từ onBleScan) + **error classify** qua TuyaErrors.describe. Thêm example/src/ui.tsx (primitives Section/Field/Btn/Console + useConsole + useRun wrapper). Auto-capture homeId/token/devId vào field giữa các bước. Method ◑/skeleton reject not_implemented → console hiện (đúng thiết kế). README +mục "Example app". ⚠️ Cần native build để chạy (getEnforcing throw nếu thiếu native). Type-review OK; chưa chạy (no toolchain). |
| 2026-06-30 | DOC + FINALIZE | ✅ | Cập nhật README (5→**12 module** + ma trận wired/◑/TODO + section Events 17 helper + Architecture 12 module + codegen constraints). Tick AC1–AC4 (code), AC5 (facade+README xong; tsc/lint deferred), AC6 chặn. INDEX cập nhật. **Feature code-complete.** Còn lại: wire native trên SDK thật (nhóm TODO/skeleton) + verify iOS bridgeless event + device-test. |
| 2026-06-30 | REVIEW Đợt 3 (B12–B15) | ✅ sạch | Workflow adversarial 4 dimension (codegen / parity / facade+event-wiring / Member native-API). **0 finding** — cả 4 reviewer trả []. Facade-wiring (rủi ro cao nhất: key-collision flat facade + event consistency) sạch; 3-mặt parity 12 module khớp; codegen safe (Member KHÔNG addListener, Matter/Mesh có + event-emitter); Member wired native khớp verbatim. |
| 2026-06-30 | DEV B15 (TuyaMesh) | ✅ scaffold / ⏳ native | Module mới (P3, **12 module** — HẾT 15 bước). Spec SIG+Tuya mesh: createSigMesh/createTuyaMesh/getMeshList + startMeshClient/stopMeshClient (proxy lifecycle) + searchSubDevices/activateSubDevice + publishMeshDps/multicastMeshDps (nodeId/localId,pcc,dps) + 3 type (MeshInfo/MeshSubDevice). **Native SKELETON có chủ đích**: mesh subsystem stateful lớn (client+device instance+listener) + iOS SIG/Tuya mesh CHƯA verbatim (note Câu hỏi mở) + note ghi 'chỉ làm nếu có thiết bị mesh' → TODO-reject + intended-call §7/§8; iOS event-emitter (3 event). Facade + meshEmitter + onMeshDeviceFound/DpUpdate/StatusChanged + TuyaEvents 3 hằng. Beacon bỏ (ít dùng). |
| 2026-06-30 | DEV B14 (TuyaMatter) | ✅ scaffold / ⏳ native | Module mới (P3, **11 module**). Spec STATEFUL: parseSetupCode→connectDevice→commissionDevice (native GIỮ SetupPayload/ConnectResult giữa call — KHÔNG round-trip object native qua JS) + startDiscovery/stopDiscovery + continueCommissioning/cancelActivator + 3 type (MatterSetupPayload/ConnectResult/PairedDevice). **Native SKELETON có chủ đích**: Matter dùng API RIÊNG (getMatterDevActivatorInstance/ConnectDeviceBuilder/CommissioningParameters/MatterActivatorCallback) + cần init Matter SDK + entitlement iOS + KHÔNG obfuscate → TODO-reject + intended-call đầy đủ từ note §5; iOS TODO (event-emitter, 3 event). Facade + matterEmitter + onMatterDeviceFound/Attestation/Error + TuyaEvents 3 hằng. KHÔNG round-trip object là quyết định thiết kế quan trọng. |
| 2026-06-30 | DEV B13 (Pairing nâng cao) | ✅ scaffold / ⏳ native | Mở rộng TuyaPairing +6 method (sub-device/gateway/QR/wired/destroyActivator). **Native = SKELETON có chủ đích** (như B7): unified ActivatorService (ActivatorMode / Zigbee+QRScanActivator+Params / IActivatorListener / IDevice / IDiscovery) package CHƯA verbatim + note device-pairing cảnh báo KHÔNG trộn classic+unified → Android TODO-reject (Promise) / no-op+event (void) kèm **intended-call** từ note; iOS stubs TODO. Spec + facade hoàn chỉnh. destroyActivator hiện dọn wifiActivator+scan. KHÔNG thêm event mới (dùng onPairingProgress step='subdevice_found/error'). |
| 2026-06-30 | DEV B12 (TuyaMember) | ✅ code | Module mới (P3, thành viên+lời mời+chuyển chủ, KHÔNG event). Spec Member/InvitationResult + 11 method (param đều an toàn keyword) + đăng ký (**10 module**) + facade. **Android WIRED:** queryMembers (queryMemberList+IThingGetMemberListCallback→MemberBean map), updateMember (overload memberId/name/admin), removeMember, cancelInvitation (cancelMemberInvitationCode), updateInvitedMember, joinHomeByCode (joinHomeByInviteCode), processInvitation. **TODO:** addMember (MemberWrapperBean construction chưa verbatim), createInvitation+getInvitationList (InvitationMessageBean field chưa rõ), transferHomeOwner (Biz FamilyManagerCoreKit package chưa verbatim). iOS TODO-reject. ⚠️ verify MemberBean getters + IThingGetMemberListCallback. |
| 2026-06-30 | REVIEW Đợt 2 (B7–B11) | ✅ + 1 fix | Workflow đa-agent adversarial (5 dimension reviewer Explore + verify-refute từng finding; 32 agent, ~1.25M tok). **27 raw → 26 dismissed (false-positive/intentional-TODO/khớp verbatim) → 1 defect thật.** parity & typemap reviewer trả [] (3-mặt + type-mapping SẠCH). **FIX:** B9 `TuyaMessageModule.deleteMessages` — `IBooleanCallback.onSuccess(result:Boolean)` sai arity (SDK thật onSuccess() KHÔNG tham số như Auth) → đổi `onSuccess() = resolve(true)`. Dismiss đáng chú ý (đều đã có ⚠️ verify/đúng thiết kế): timer actions/getTimerList/updateTimer-id, message TODO enum/bean, home weather-detail, pairing timeout-unit (giây cho ActivatorBuilder.setTimeOut vs ms cho bean — ĐÚNG), iOS invalidate (stub chưa có state). |
| 2026-06-30 | DEV B11 (TuyaPairing combo+auto-token) | ✅ code | Mở rộng TuyaPairing (HẾT Đợt 2). Spec +startBleWifiPairing/stopBleWifiPairing/startWifiPairingAuto (dùng classic API cho nhất quán). **Android WIRED:** startBleWifiPairing (cache scan bean theo uuid → auto getActivatorToken → MultiModeActivatorBean{uuid,deviceType,ssid,pwd,token,homeId,timeout} → getActivator().newMultiModeActivator().startActivator + IMultiModeActivatorListener onSuccess/onFailure/onActivatorStatePauseCallback→onPairingProgress step='combo_stage'); stopBleWifiPairing (newMultiModeActivator().stopActivator(uuid)); startWifiPairingAuto (getActivatorToken→doWifiPairing refactor dùng chung với startWifiPairing); **invalidate()** dọn wifiActivator+scan. iOS stubs TODO-reject. Facade +3 method. ⚠️ verify package MultiModeActivatorBean/IMultiModeActivatorListener/PauseStateData + PauseStateData getters (hiện toString). |
| 2026-06-30 | DEV B10 (TuyaHome weather+listener) | ✅ code | Mở rộng TuyaHome (KHÔNG module mới). Spec +WeatherSketch + getHomeWeatherSketch(homeId,lon,lat)/getHomeWeatherDetail + start/stop HomeChange & HomeStatus listener + addListener/removeListeners. **TuyaHome thành event-emitter** (iOS: NSObject→TuyaEventEmitter, supportedEvents `onHomeChange`; Android: emit qua RCTDeviceEventEmitter). **Android WIRED:** getHomeWeatherSketch (getHomeManagerInstance().getHomeWeatherSketch(lon,lat,IIGetHomeWetherSketchCallBack)→WeatherBean), startHomeChangeListener (IThingHomeChangeListener: onHomeAdded/Invite/Removed/InfoChanged/SharedDeviceList/SharedGroupList/ServerConnect), startHomeStatusListener (IThingHomeStatusListener: onDevice/Group/Mesh Added/Removed), stop* + invalidate cleanup. **getHomeWeatherDetail TODO** (DashBoardBean field chưa rõ). iOS stubs TODO-reject. Facade + TuyaEvents.HOME_CHANGE + homeEmitter + onHomeChange + HomeChangeEvent/WeatherSketch export. ⚠️ verify import WeatherBean/IIGetHomeWetherSketchCallBack/IThingHomeChange+StatusListener (package) + unRegisterHomeStatusListener. |
| 2026-06-30 | DEV B9 (TuyaMessage) | ✅ code | Module mới (gộp push+message+DND, KHÔNG event). Spec 21 method (đổi `id`→`dndId` vì keyword ObjC) + đăng ký (**9 module**) + facade. **Android WIRED:** registerDevice (getPushInstance().registerDevice), setPushStatus, get/setDndStatus, addDnd/addOnceDnd/modifyDnd/removeDnd (DND-write, devIds→JSON), deleteMessages (IBooleanCallback), markMessagesRead (no-op Android — note xác nhận không có API). **TODO:** message-list/byType/detail + getMessageHasNew + getPushStatus + push-by-type + getDnd/OnceDndList (cần enum MessageType/PushType + bean MessageListBean/MessageHasNew/PushStatusBean/DeviceAlarmNotDisturbVO verbatim). iOS TODO-reject. ⚠️ verify package push/message + thứ tự bit loops. |
| 2026-06-30 | DEV B8 (TuyaTimer) | ✅ code | Module mới TuyaTimer: spec (inputJson + 5 method, KHÔNG event) + đăng ký (**8 module**) + facade. **Android WIRED:** addTimer/updateTimer (ThingTimerBuilder từ inputJson), removeTimer + updateTimerStatus (getTimerInstance().updateTimerStatus + TimerUpdateEnum DELETE/OPEN/CLOSE). **getTimerList TODO** (bean TimerTask field chưa verbatim). iOS TODO-reject. ⚠️ verify package timer (ThingTimerBuilder/TimerDeviceTypeEnum/TimerUpdateEnum) + **format setActions** (time nằm trong actions — best-guess). Type-review OK. |
| 2026-06-30 | DEV B7 (TuyaScene) | ✅ scaffold / ⏳ native | Module mới TuyaScene (Đợt 2): **spec đầy đủ 26 method** (codegen-safe: bỏ union → value:string, object param → `paramsJson` string, đổi `operator`→`op` vì keyword C++), **đăng ký TurboTuyaPackage (7 module)**, facade phẳng + sub-module export + 5 type + `onSceneChange` event. **Native = SKELETON có chủ đích** (Android+iOS TODO-reject) vì scene SDK (getSceneServiceInstance/NormalScene/SceneCondition/builder + generic IResultCallback) package+cách lắp CHƯA verbatim (note "Câu hỏi mở"). Mỗi method ghi **intended-call** (từ note) để wire trên máy có SDK. Parity 3-mặt khớp; codegen sạch. |
| 2026-06-30 | QUALITY (post-review) | ✅ | Xử lý các điểm quality để ngỏ: package.json bỏ entry `react-native.config.js` thừa + `jsSrcsDir`→`src/specs` (codegen chỉ quét 6 spec); `UserResult` +`countryCode` (iOS điền u.countryCode, Android ''); JSDoc thirdLogin (extraInfo Google) + getCurrentUser/syncUserInfo (reject 'no_user'); iOS `getSdkVersion`→'thingsmart:7.5.0' (có version). TuyaReject helper giữ làm convention (áp dụng dần). **Còn lại duy nhất: iOS bridgeless event — verify Xcode.** |
| 2026-06-30 | REVIEW Đợt 1 | ✅ | Workflow 4-agent (TS/Android/iOS/parity): **KHÔNG blocker, parity 3-mặt 100%, codegen/compile sạch, errors.ts an toàn noUncheckedIndexedAccess**. ĐÃ FIX: OTA listener leak→`invalidate()` (Ota+Device dọn service/listener); Device `registerDeviceListener` idempotent (unRegister trước); `publishDpsAwaitAck` resolve khi onDpUpdate **khớp dpId** vừa publish; Auth `syncUserInfo` null-guard; Pairing BLE scan đổi sang `object : BleScanResponse` (bỏ rủi ro SAM); comment "5"→"6" module. **Residual (không fix được ở đây):** ⚠️ iOS RCTEventEmitter+TurboModule **bridgeless** — event có tới JS không, verify Xcode (fallback codegen EventEmitter). Quality ghi nhận: UserResult thiếu countryCode, getSdkVersion hằng, files[] có react-native.config.js không tồn tại. |
| 2026-06-30 | DEV+TEST B5+B6 | ✅ code / ⏳ verify | **B5 TuyaOta** (module mới): spec NativeTuyaOta + Android `TuyaOtaModule` (newOTAServiceInstance: getFirmwareUpgradeInfo→UpgradeInfo[], startFirmwareUpgrade lọc theo type, cancel/confirmWarning/get+setAutoUpgradeSwitch; IDevOTAListener→event onOtaProgress/StatusChanged/Success/Failure) + iOS TODO-reject + register TurboTuyaPackage + facade/4 event helper. ⚠️ verify IDevOTAListener/IGetOtaInfoCallback/UpgradeInfoBean. **B6 TuyaHome.updateHome/dismissHome** Android wired (IResultCallback); iOS TODO-reject. **Đợt 1 (B1–B6) xong code.** Type-review OK; build deferred. |
| 2026-06-30 | DEV+TEST B4 | ✅ code / ⏳ verify | Mở rộng TuyaDevice theo verbatim device-control. Spec +DeviceSnapshot + rename/remove/reset/getWifiSignal/queryDp/getDeviceSnapshot/isDeviceOnline/isCloudConnected/publishDpsWithMode/WithChannels/publishDpsAwaitAck/sendCacheDps/ble*. **Android wired:** rename/remove/reset/queryDp(getDp)/getDeviceSnapshot(DeviceBean)/isDeviceOnline/isCloudConnected/publishDpsWithMode(ThingDevicePublishModeEnum)/**publishDpsAwaitAck** (instance riêng + one-shot IDevListener + Handler timeout). TODO-verify: getWifiSignal(WifiSignalListener)/publishDpsWithChannels(CommunicationEnum)/sendCacheDps/ble*. iOS: TODO-reject toàn bộ method mới (giữ parity; impl device-control iOS là effort riêng). Facade + DeviceSnapshot export. Type-review OK; build deferred. ⚠️ verify import ThingDevicePublishModeEnum. |
| 2026-06-30 | DEV+TEST B3 | ✅ code / ⏳ verify | Mở rộng TuyaAuth theo **verbatim** note user-account. Spec: +syncUserInfo/updateNickname/updateTempUnit/updateTimeZone/updateAvatarByUrl/cancelAccount/resetEmail+PhonePassword/bind*/getLoginTerminals/terminateSession/addListener + UserResult giàu hơn (tempUnit/timezoneId/headPic/mobile/regionCode). Android: wired hết bằng IResultCallback/IReNickNameCallback/IResetPasswordCallback/TempUnitEnum + `setOnNeedLoginListener`→event onSessionExpired; bind*/multi-device = TODO-verify. iOS: TuyaAuth→TuyaEventEmitter, profile/reset/cancel/logout/terminals wired (ThingSmartUser) + observe notification session-invalid; login email vẫn TODO. Facade + onSessionExpired thêm vào index.tsx. **Type-review OK; build deferred** (no toolchain). ⚠️ verify import: TempUnitEnum/IReNickNameCallback/IResetPasswordCallback/INeedLoginListener. |
| 2026-06-29 | DEV B2 | ✅ code | C1 iOS event infra: tạo `ios/Common/TuyaEventEmitter.{h,mm}` (`: RCTEventEmitter`; emit/supportedEvents/startObserving). `TuyaDevice`/`TuyaPairing` đổi superclass→TuyaEventEmitter, bỏ no-op addListener/removeListeners (kế thừa), thêm supportedEvents. Android/JS không đổi. **Verify Xcode** (RCTEventEmitter+TurboModule new-arch; fallback codegen EventEmitter nếu lỗi). |
| 2026-06-29 | TEST B1 | ⏳ deferred | `yarn typecheck`/`yarn test` KHÔNG chạy được: không có `node_modules`, `yarn install` timeout mạng (ETIMEDOUT) — môi trường không ra registry. **Đã type-review thủ công** (tsconfig `noUncheckedIndexedAccess`) → bắt & sửa 1 lỗi: `if(MAP[code]) return MAP[code]` không narrow → đổi sang biến cục bộ `cloud`/`sdk` trong `categoryOf`. Logic 8 test case khớp impl. Chạy thật khi có mạng. |
| 2026-06-29 | DEV B1 | ✅ code | C0: `src/errors.ts` (TuyaErrors: classify/isRetryable/describe; types TuyaError/TuyaErrorInfo/Category/Domain; bảng mã từ note error-codes; 1004 phân biệt theo domain; range 50500–50516) + export index.tsx + native reject helper Kotlin/ObjC + unit test errors.test.ts. **Chưa verify** (no node_modules/network). → `/test`. |
| 2026-06-29 | PLAN | ✅ | Tạo plan/context/progress (15 bước: C0/C1 prereq + module theo 3 đợt P1–P3) + đăng ký INDEX. Dựa trên research full-surface (11 note). Chưa code. |

## Vấn đề đang chặn (Blockers)
- **Build/test native:** cần JDK17 + Android SDK (Android) / Mac + Xcode (iOS) — máy hiện tại chưa có (như m1-tuya-sdk-library).
- **dpId/schema bồn tắm đá:** cần thiết bị thật + productId từ client → chặn AC device của B4/B7/B8.
- **Verify chữ ký:** nhiều API có 2 thế hệ + iOS chưa verbatim → verify trên reference khi code từng bước.
