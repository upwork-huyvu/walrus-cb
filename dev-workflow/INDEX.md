# Bảng điều phối tiến trình - Tuya Smart Ice Bath App

> Nơi DUY NHẤT trả lời "dự án đang ở đâu?". Mọi feature đều phải có 1 dòng ở đây.
> `/plan` thêm dòng mới; `/dev` `/test` `/fix-plan` cập nhật cột Phase/Trạng thái.

- **Cập nhật lần cuối:** 2026-07-16 (`m1-pairing-radar-discovery` → TEST: **revise #1 hiện thực xong** (B7-B9): 3 mode tường minh (Android EZ·AP·BLE / iOS AP·BLE), Wi-Fi input theo mode (EZ dropdown · AP tự nhập), hướng dẫn đánh số. **Xoá thiết kế EZ‖BLE song song** ⇒ rủi ro lớn nhất của plan biến mất. jest **261/261**. Còn B6 verify máy thật)

> ⚠️ **M1 đã MỞ RỘNG scope (2026-06-28).** Bản brief mới đưa thêm **Part A (thư
> viện npm Tuya SDK)**, **Part C (backend NestJS + quản lý user)** và **Part D
> (admin web)** vào M1 (trước đây C/D thuộc M2). Ngân sách $250 ban đầu **cần ET lại**.
> M1 nay được chia nhỏ thành các feature A/B/C/D bên dưới.

> ⚠️ **MẠNG ĐÃ THÔNG (2026-06-30).** Registry npm trước đây bị E503/chặn → nhiều feature ghi "tsc/jest deferred (no
> node_modules+503)" và đánh dấu code dựa trên **type-review thủ công**. Verify thật trên `m1-mobile-dashboard` vừa
> phát hiện 2 file ("code xong" trong progress) **chưa từng tồn tại trên đĩa**. → Mọi feature còn dòng "deferred" cần
> chạy `npm install` (hoặc `yarn`/`corepack yarn` tùy app) + `tsc`/`jest`/`eslint` thật trước khi tin progress.md cũ.
>
> 🐛 **ROOT CAUSE đã sửa (2026-06-30):** `.gitignore` gốc có rule `**/lib/` (ý định: ignore build output
> `packages/tuya-react-native/lib/`) nhưng glob quá rộng → nuốt luôn `apps/mobile/src/lib/` và `apps/admin/lib/`
> (2 thư mục **source thật**, không phải build output). Đây chính là lý do `lib/format.ts`, `lib/debounce.ts`
> (m1-mobile-dashboard) và `lib/auth.ts` (m1-admin-push) liên tục bị coi là "thiếu trên đĩa" giữa các session - file
> được tạo nhưng **không bao giờ commit**, nên không sống sót qua session/worktree mới. Đã thu hẹp rule thành
> `packages/*/lib/`. `apps/mobile/src/lib/*` sống sót (untracked). **`apps/admin/lib/*` thì KHÔNG** - file thật sự
> chưa từng tồn tại lại trên đĩa (không chỉ untracked) cho tới khi build-thử 2026-06-30 phát hiện `tsc`/`next build`
> fail 100% (mọi route import `@/lib/api`, `@/lib/auth` không resolve) và dựng lại theo đúng contract README/code gọi.
> **2 thư mục `lib/*` này giờ cần `git add` ở lần commit tới**, nếu không sẽ tái diễn lỗi này.

## Đang làm / Tồn đọng
| Feature (slug) | Milestone | Phần | Phase | Trạng thái | Cập nhật | Thư mục |
|---|---|---|---|---|---|---|
| m1-pairing-radar-discovery | M1·B (pairing UX) | mobile (chỉ JS) | TEST | ✅ **B1-B5 + B7-B9 CODE XONG & XANH** (tsc 0 · eslint 0 lỗi · **jest 261/261**) ⇒ **cài lại APK là test được** (chỉ JS, không rebuild native). 🔴 **Còn B6: verify SM-A325F thật** - ưu tiên: **AC1** chọn mode **BLE** → phải HIỆN prompt quyền Bluetooth (blocker gốc; EZ/AP cố ý **không** xin quyền BLE) → **AC8** dropdown Wi-Fi của EZ quét ra mạng + chọn xong thu lại → **AC7/AC2** blip trên radar → **AC9** AP phải vào với 2 ô TRỐNG (chỗ dễ hồi quy nhất). --- **revise #1 (client review B1-B5) ĐÃ HIỆN THỰC XONG (B7+B8+B9)** - 4 thay đổi: (1) **bỏ mode `auto`**, còn 3 mode - **Android EZ·AP·BLE, iOS AP·BLE**, default Android=EZ / iOS=AP; (2) **EZ: Network name thành dropdown có scan bên trong**, chọn xong collapse; không quét được → prefill mạng đang nối; (3) **AP: ssid+password TỰ NHẬP, cấm prefill/scan**; (4) cần **hướng dẫn từng bước ĐÁNH SỐ** cho từng mode (AP phải nêu bước nối Wi-Fi của thiết bị). 🟢 **Hệ quả kiến trúc tốt:** mỗi mode giờ **đúng 1 kênh** ⇒ **thiết kế EZ‖BLE song song bị xoá** ⇒ **rủi ro số 1 của plan tự biến mất** (không doc nào bảo đảm 2 activator chạy song song an toàn) + `startScan()` mất nhánh khó test nhất. 🟡 **Mất:** iOS không còn "vào màn là radar quay luôn" như ảnh Smart Life (default AP cần nhập Wi-Fi trước) - **client đã chốt, không phải bug**. ✅ **Client đúng & khớp doc ở điểm (3):** lúc pair AP máy đang nối **hotspot thiết bị** ⇒ "Wi-Fi đang kết nối" = `SmartLife-xxxx` mà SDK cần SSID **của ROUTER** ⇒ prefill ở AP = điền sai gần chắc chắn. Plan: AC4/AC5 viết lại + thêm **AC8/AC9/AC10** + thêm **B7/B8/B9**, B6 lùi cuối. **B1/B2/B4 (`blePermissions`·`radarModel`·`RadarView`) KHÔNG đụng** - dùng lại nguyên. **B7** `pairingModes.ts` viết lại (26 test): **`channel` số ít** (kiểu dữ liệu chặn việc gộp kênh trở lại), `wifiInput`, `steps[]`. **B8** `wifiDropdownCard()` (EZ: bấm là **tự quét luôn**, chọn → **collapse**, vẫn gõ tay được cho mạng ẩn) vs `wifiManualCard()` (AP: 2 ô trống + banner "đây là Wi-Fi NHÀ, không phải hotspot SmartLife"); **chặn prefill khi `wifiInput==='manual'`** + **xoá ssid/pass khi đổi mode** (EZ→AP mà giữ nguyên thì AP thừa hưởng đúng cái prefill nó vừa cấm). **B9** `startScan()` rút theo `mode.channel`, **xoá `wifiChannelOf()` + nhánh "kênh Wi-Fi hỏng nhưng BLE vẫn quét"**; `stepsCard()` đánh số; radar chỉ nhắc **bước 1**. 📌 Nhãn "recommended" chỉ gắn **iOS/AP** (có nguồn Tuya) - Android EZ **không** gắn vì doc nói EZ tỉ lệ thành công **thấp hơn** AP ⇒ gắn vào là tự bịa. --- *(B1-B5:* (tsc 0 · eslint 0 lỗi · **jest 258/258**, +62 so với baseline 196) ⇒ **cài lại APK là test được thật** (chỉ JS, không rebuild native). 🔴 **Còn B6: verify SM-A325F thật** - ưu tiên **AC1** (Android 12+ phải HIỆN prompt quyền Bluetooth; không thấy prompt = mọi thứ còn lại vô nghĩa) → AC7/AC2 (sensor có lên blip không). **B1** `blePermissions.ts` (9 test): kết quả **có kiểu** `denied`/`blocked`/`unsupported` (boolean thì mất phân biệt "hỏi lại được" vs "phải vào Settings" - đúng cái ngõ cụt đang sửa); API 31+ xin SCAN+CONNECT+**LOCATION**, API ≤30 chỉ LOCATION, iOS no-op. **B2** `radarModel.ts` (23 test): blip 2 nguồn, dedupe, **toạ độ tất định qua hash FNV-1a** (random ⇒ blip nhảy loạn mỗi event scan, không chạm trúng), first-seen order, cap 8, `isPairableBlip` chỉ true với BLE. **B3** `pairingModes.ts` (23 test): 4 mode auto→ez→ap→ble, default auto, `auto=['ez','ble']` **không AP** (test khoá cứng); **iOS bỏ EZ, iOS `auto`=BLE thuần `needsWifi:false` ⇒ vào màn là radar luôn** (đúng ảnh client - ảnh chụp iPhone); hướng dẫn AP **tách theo nền tảng**. **B4** `RadarView.tsx` (7 test, **test component đầu tiên của repo**). **B5** ráp `PairingScreen`: bỏ step `prepare`+`found`, `startScan()` chạy mọi kênh song song + xin quyền BLE trước, blip → **Modal popup 3 nhánh**, dropdown 4 mode; dọn code chết (`pairingOrb`/`radarWave1-3`). 🐛 **2 bug test bắt được:** `Animated.loop`+`act()` → jest **TREO IM** (cần `useFakeTimers`); `findAllByType(Pressable)` trả **RỖNG** → 1 test **pass rỗng**, đã đổi sang lọc testID. 📌 **Research 2026-07-16** ([tuya-ios-ap-mode-pairing.md](../docs/research/tuya-ios-ap-mode-pairing.md)): **AP legacy KHÔNG cần entitlement Apple** ("Hotspot capability"+`ThingSmartHotspotCredentialKit` **chỉ thuộc AP Plus**) ⇒ iOS chọn AP là **thoát thật** khỏi bế tắc EZ-multicast; Tuya nói thẳng *"we recommend that you use the AP mode instead of the Wi-Fi EZ mode"*; **Android SDK tự nối hotspot, iOS thì KHÔNG**; đèn **AP chậm / EZ nhanh** (hướng dẫn cũ của tôi thiếu hẳn bước này); ssid/password là **của ROUTER** không phải hotspot thiết bị. 🔴 **RỦI RO chưa gỡ:** iOS/AP có thể lấy token **khi đã mất mạng** (`startWifiPairingAuto` tự lấy token ngay lúc chạy, mà lúc đó máy đang ở hotspot thiết bị) → B6 test riêng; gỡ được **không cần đụng native** (lib đã có `getPairingToken()`+`startWifiPairing()`). --- Client muốn màn pairing **radar giống Smart Life** (ảnh "Searching for nearby devices…"): blip hiện **trên sóng radar** → chạm → popup → mới authen cloud; **bỏ hỏi mode** ở luồng chính nhưng **có dropdown** 4 lựa chọn (Auto/EZ/AP/BLE) nội dung riêng từng cái. · 🔴 **BLOCKER PHÁT HIỆN:** Android **chưa bao giờ xin `BLUETOOTH_SCAN` runtime** - manifest `AndroidManifest.xml:18-19` **có khai** nhưng **không chỗ nào** gọi `PermissionsAndroid.request()` (cả repo chỉ xin `ACCESS_FINE_LOCATION` trong `wifiScanner.ts:48-57` cho việc quét Wi-Fi, không dính pairing). `targetSdkVersion=36` ⇒ Android 12+ coi là **runtime permission** ⇒ `startLeScan` **im lặng trả 0 kết quả** ⇒ radar quay 120s rồi "No Walrus devices found". **Nhiều khả năng đây là lý do luồng BLE-discovery-first (B9 của `m1-fix-wifi-pairing`) chưa từng được thấy chạy.** Thêm: `BLUETOOTH_SCAN` khai **không có** `neverForLocation` ⇒ Android 12+ đòi cả `ACCESS_FINE_LOCATION` + **GPS phải bật** mới có kết quả scan. · **3 ràng buộc vật lý đã chốt với client** (ép lại thiết kế): (1) **EZ không discovery được** (phát mù) ⇒ radar phải đứng **sau** bước xác nhận Wi-Fi; (2) **EZ ‖ AP bất khả** (AP đòi nối hotspot thiết bị, EZ đòi ở Wi-Fi nhà - một máy không ở 2 mạng) ⇒ **KHÔNG** làm chuỗi auto EZ→AP→BLE (~6 phút quay mù) ⇒ chốt **Auto = EZ ‖ BLE song song**, **AP chỉ qua dropdown**; (3) EZ chỉ lộ device qua `onStep(device_find)` lúc SDK **đã tự bind** ⇒ **blip EZ không gate được bằng tap**, chỉ blip BLE mới "chạm → pair". · 💡 **Vì sao Smart Life không hỏi mode:** nó dùng **unified `ActivatorService`** - `startSearch:` nhận **LIST** type model → quét mọi kênh song song → `didFindDevice` **kèm `activatorType`** → `startActive:` đúng type ⇒ **mode được SUY RA, không hỏi**. App mình ở **thế hệ cũ** (mỗi mode 1 hàm) ⇒ buộc phải hỏi. **Scope client chốt: KHÔNG wire unified** (nợ kỹ thuật có chủ đích) → đi legacy + route bằng `isCombo` (B8 đã đẩy sẵn lên JS). · 6 bước: **B1 permission gate (blocker, làm trước)** → B2 `radarModel` (gom blip 2 nguồn, toạ độ tất định) → B3 `pairingModes` (registry + nội dung dropdown) → B4 `RadarView.tsx` (SVG, **`react-native-svg` đã có sẵn** ⇒ không thêm dep) → B5 ráp `PairingScreen` → B6 verify SM-A325F. **Chỉ đụng JS ⇒ không rebuild native, cài lại APK là test.** · ❓ **Chưa xác nhận sensor có BLE beacon** (quyết định AC7): test 30s = mở Smart Life vào màn radar, **không** bấm "Add manually", xem sensor có tự hiện. | 2026-07-15 | [↗](m1-pairing-radar-discovery/progress.md) |
| m1-fix-notifications | M1·B (fix) | mobile+backend | TEST | ✅ **B1-B7 CODE XONG & CI XANH** (mobile tsc0/jest**196**/eslint0 · backend jest**76**/build0). **B1** diag `[NOTIF-DIAG]` (persist + fcm-history, `__DEV__`, chờ log device). **B2** badge nhảy khi nhận push foreground (`onForegroundMessage(onReceive)`, ref tránh stale-closure). **B3** read-state **per-user + per-message** (`readStore` key `...read.<uid>.v2`, cap 500, migration coi noti cũ=đã đọc) thay timestamp global. **B4** backend log **DURABLE** cho mọi `input.uids` (kể cả chưa có token). **B5** `lib/time.ts parseWhen` Hermes-safe thay `new Date(spaceString)`. **B6** `git rm` **19 file `* 2.*`** (đã commit nhầm, stale). 🔴 **Còn: device checklist (AC1-4) + deploy backend + đọc log chốt #1/#2** (root cause thật chưa xác nhận bằng dữ liệu). --- 🐛 **3 bug thông báo:** (1) vào không thấy noti mới · (2) đã đọc → logout/login thành mới lại · (3) badge số Account không hiện/không nhảy. **Điều tra sâu bằng workflow `notif-root-cause` (10 agent + phản biện, provider=FCM) → LẬT chẩn đoán ban đầu:** ~~#1 do parse Hermes~~ **SAI** - uid linkage ĐÚNG theo thiết kế, noti FCM ts đúng → #1 thật = **FCM-history rỗng** (gửi lúc chưa có token sống/token prune/`PUSH_API_KEY` rỗng/FCM chưa config → không ghi `notification_logs`). #2 **KHÔNG có cơ chế xác định trong code** (phản biện loại 5/6 giả thuyết: lastReadAt không bị clear, đơn điệu tăng, ts FCM đúng, dupe file giống hệt) → còn nghi **AsyncStorage mất persist (always-unread)** hoặc môi trường → **cần device-log**. #3 CONFIRMED (`onForegroundMessage` thiếu `refreshUnread`). Parse Hermes DEMOTED (chỉ kênh Tuya phụ). Defect thật: read-state key **global không per-user** + vứt `hasNotRead` Tuya; `getMessageHasNew` native wire mà không dùng. **Plan 7 bước (reorder):** B1 diagnostic-first #1&#2 → B2 badge realtime → B3 read-set per-user → B4 FCM-history bền → B5 parse (demote) → B6 dọn `* 2.*` → B7 verify. **Chờ gate ① + user chốt: (a) migration read v1→v2, (b) có đổi hành vi backend log không.** | 2026-07-13 | [↗](m1-fix-notifications/progress.md) |
| m1-fix-wifi-pairing | M1·B (fix) | mobile+lib | TEST | ✅ **B1–B4,B6,B8,B9 CODE XONG & TEST XANH** (tsc 0 · eslint 0 · **jest 178/178** · Kotlin ✅ · iOS ✅). **B8** đẩy `bleType`(iOS int)/`configType`(Android str)/`isCombo` lên JS thay `deviceType:0` + wire `continueConfigSecurityLevelDevice` (thiết bị `*Security` hết treo). **B9** đảo UX sang **BLE-discovery-first giống Smart Life**: Add device → tự quét BLE → thiết bị combo **tự hiện** → chạm → hỏi Wi-Fi → pair; EZ/AP thành "Add manually"; `pairBle` (BLE thuần). Enum route verbatim (header SDK + javap). 🔴 **Còn:** B5 (Apple duyệt) · **B7+AC8/AC9 cần thiết bị thật** (rebuild app vì đụng native). --- *(chi tiết B1-B6 bên dưới)* **B1** mọi reject native qua `TuyaReject` (helper **có sẵn mà chưa ai dùng**) + `NSError.code` thật. **B2** `describeError()` giữ message native; mã phi-số không còn bị đẩy vào bảng mã (+test hồi quy chốt hết `Unknown error`). **B3** `pairingLog.ts` (redact password) + "Copy diagnostics"; **sửa 3 bug iOS**: step callback **vứt mất `error`** (SDK báo lỗi join-Wi-Fi qua đúng đường này!), `step==4`=**TimeOut** mà UI vẫn nhích checklist, `deviceStateError:` chưa implement. Enum thật từ header: `1=Found 2=Registed 3=Intialized 4=TimeOut`. **B4** iOS mặc định **AP mode** (Android giữ EZ để B7 còn quan sát được bug thật). **B6** `pairingPreflight.ts` chặn 5GHz/6GHz + SSID rỗng; phát hiện `getFrequency()` **Android-only** → iOS nói thẳng "không kiểm được băng tần". 🔴 **CÒN LẠI cần người:** **B5** nộp đơn Apple (entitlement **cố ý chưa thêm vào repo** - thêm sớm sẽ fail ký, phá build; xem `apps/mobile/PAIRING_MULTICAST_SETUP.md`) · **B7** test ice tub thật. ❓ **Android fail vì gì vẫn CHƯA rõ** (nghi 5GHz). · 🐛 **BUG khách: pair Wi-Fi EZ fail cả iOS+Android, lỗi rỗng `[sdk:pairing_error] Unknown error.`** · **(1) Bug hiển thị lỗi - truy vết đủ 4 mắt xích:** `TuyaPairing.mm:104` reject literal `@"pairing_error"` (vứt `NSError.code`) → `pairing.ts:164-167` `describeError()` nuốt luôn `e.message` → `errors.ts:145` code phi-số → `'unknown'` → `errors.ts:198` in ra đúng chuỗi trong ảnh. Helper `TuyaReject.h/.kt` **có sẵn mà Pairing không dùng**. · **(2) iOS root cause XÁC NHẬN bằng doc Tuya** ([research 2026-07-10](../docs/research/tuya-wifi-ez-pairing-failure.md)): iOS 14.5+ **không thể gửi gói EZ** nếu thiếu entitlement `com.apple.developer.networking.multicast` → **Apple phải duyệt 3-5 ngày**, form đòi App Store URL (app chưa phát hành ⇒ rủi ro). Smart Life pair được vì **Tuya đã có entitlement**. · **(3) ❌ ĐÍNH CHÍNH:** giả thuyết "Android thiếu `CHANGE_WIFI_MULTICAST_STATE`" **SAI** - quyền này **đã có** trong **merged manifest** (AAR `thingsmart:7.5.6` tự khai); lần đầu grep nhầm manifest **nguồn**. ⇒ **Android vẫn chưa rõ nguyên nhân**, nghi **Wi-Fi 5GHz** (thứ duy nhất giải thích cả 2 nền tảng). 📌 *Bài học: permission Android → luôn đọc merged manifest.* · **(4) ĐỔI HƯỚNG:** Tuya khuyến nghị **AP mode** thay EZ (né entitlement + hỗ trợ router 2.4+5GHz) → B4 thành đường chính; AC1/AC2 viết lại "**EZ hoặc AP**", không phụ thuộc Apple. · 7 bước: B1-B3 observability TRƯỚC → B4 AP mode → B5 nộp đơn Apple (song song) → B6 preflight → B7 verify device thật. **Chờ gate ① duyệt plan (bản revise).** | 2026-07-10 | [↗](m1-fix-wifi-pairing/progress.md) |
| m1-mobile-home-device-flow | M1·B3+B4+B5 | mobile+lib | TEST | **B0–B14 CODE XONG** - điều hướng SmartLife + **REDESIGN theo design client (2026-07-03)**: auth social-first (logo SVG) + country dropdown (bảng Tuya WE) · intro 4 slide SAU login đầu (cờ local) · pairing 5 màn + nhớ Wi-Fi · **bottom tab 5 mục** (Device/Reminder/Shop/Help/Account) + Reminder(filter 90d)/Shop/Help(FAQ 15 QA) · auto "My Home" · **B14 Account: Change password (Tuya reset-OTP `resetEmailPassword`) + Notifications ĐỌC MESSAGE CENTER per-user (native `getMessageList` wired verbatim javap, compileKotlin ✅)** · UI đã dịch English. Verify: tsc 0 · eslint 0 · jest 61/61. **APK mới ĐÃ CÀI SM-A325F** - CHƯA demo (máy khoá), **B14 CHƯA COMMIT**. Chờ: video intro client · pair bồn thật · iOS (Mac). ⚠️ iCloud tạo file `* 2.*` trong build/ gây fail build - dọn bằng find -delete | 2026-07-03 | [↗](m1-mobile-home-device-flow/progress.md) |
| m1-tuya-sdk-library | M1·A | mobile/lib | DEV | in_progress (audit ✅ API Android đúng; **build-ready wired** aar+gradle.properties+Maven repos, BLE pairing + iOS RCT_EXPORT_MODULE done; **JDK17+Android SDK đã có sẵn trên máy (xác nhận 2026-06-30, note cũ "máy đang JDK8" SAI/lỗi thời)** - còn console SHA-256/DC + iOS 17 method) | 2026-06-30 | [↗](m1-tuya-sdk-library/progress.md) |
| m1-tuya-sdk-expansion | M1·A+ | mobile/lib | TEST | **BUILD XANH 2026-06-30** - `:jimmy-vu_react-native-turbo-tuya:compileDebugKotlin` + `:app:assembleDebug` **BUILD SUCCESSFUL** (APK `app-debug.apk` 156MB). 12 TurboModule compile sạch. **ĐÍNH CHÍNH chẩn đoán "thiếu module → cần console" (SAI):** quét lại đủ 55 artifact → mọi class ĐỀU CÓ trong `thingsmart:7.5.6`; lỗi thật = **8 import sai package + API drift** (enum `THING_AP/EZ`, `ThingTimerBuilder.Builder`, `IDevOTAListener.firmwareUpgradeStatus`, `MemberBean.getNickName/getMemberStatus`, DND không có trên Android `IThingPush` → stub…). Xác minh bằng `javap` trên SDK thật → [docs/research/tuya-android-sdk-missing-modules.md](../docs/research/tuya-android-sdk-missing-modules.md). Còn: chạy thiết bị thật verify runtime + hoàn thiện stub (DND/joinByCode/getTimerList…) + console SHA-256 cho SDK init | 2026-06-30 | [↗](m1-tuya-sdk-expansion/progress.md) |
| m1-backend-scaffold | M1·C1 | backend | DEV | in_progress (B1–B5 + **Tuya token LIVE ✅** Central EU; **AC3 DB LIVE ✅ 2026-07-02** - Prisma nối Supabase + baseline `0_init` applied. DB đã provision sẵn từ trước (db push, +1 admin `admin@walrus.app`) → dùng baseline né reset. Còn `TUYA_APP_SCHEMA` comment) | 2026-07-02 | [↗](m1-backend-scaffold/progress.md) |
| m1-backend-user-mgmt | M1·C2 | backend | DEV | **`/users` LIVE ✅🟢 2026-07-02 (Option B)** - pipeline quản lý user chạy end-to-end: `GET /users` → **total:3** (`imax`/`showroom`/`huyquoc` `we...`) + hiển thị đủ trên admin web. **Root cause đã giải:** DC MISMATCH - account App SDK route **Western Europe** (do NGÀY TẠO appKey, không countryCode nào cứu; App SDK không có Customize Rules), project cũ ở Central Europe → rỗng. **Fix = Option B:** client chuyển project → Western Europe + backend `TUYA_OPENAPI_ENDPOINT=openapi-weaz.tuyaeu.com`. Backend code KHÔNG đổi. (Central Europe muốn thì cần ticket Tuya.) | 2026-07-02 | [↗](m1-backend-user-mgmt/progress.md) |
| m1-backend-admin-auth | M1·C3 | backend | DEV | in_progress (B1–B3 code xong; **LIVE ✅ 2026-07-02** - `admin@walrus.app` có trong Supabase Auth (confirmed) + allowlist `admin_users` + đã sign-in 2026-07-01 → full auth stack thông) | 2026-07-02 | [↗](m1-backend-admin-auth/progress.md) |
| m1-admin-web | M1·D | admin | DEV | in_progress (B1–B4 code xong; **boot LIVE ✅ 2026-07-02** - Next 16 chạy port 3001, `/login` 200, đọc `.env.local`. Trang `/users` cần backend có `TUYA_APP_SCHEMA`) | 2026-07-02 | [↗](m1-admin-web/progress.md) |
| m1-mobile-auth | M1·B2 | mobile | TEST | code_done - **B1–B4 XONG** (auth.ts adapter+mock · AuthScreen login/register email+OTP+Google/Apple scaffold · useAuth + App gating splash→home/auth + onSessionExpired→auth + logout · Welcome→auth). `tsc`/`jest` deferred (no node_modules+503). Google/Apple cần native SDK (idToken). AC5 device chờ build. Chốt: Tuya account=định danh M1; Supabase sau. **B4 Google → hoàn thiện ở `m1-mobile-google-login`** | 2026-06-30 | [↗](m1-mobile-auth/progress.md) |
| m1-admin-royal-redesign | M1·D | admin+backend | TEST | **B1–B5 code XONG + verified** (admin tsc 0 · eslint 0 · backend jest 26/26 · browser 4 khu vực đẹp). Reskin admin "Gilded Noir" (royal vàng-đen): B1 globals.css+next/font(Cormorant+Inter) · B2 AdminShell sidebar · B3 reskin login. B4 admin-mgmt list+xoá (`GET/DELETE /admin/users`, chặn tự-xoá 403). B5 gửi nhiều user/tất cả (`SendPushDto` uids[]/all + loop per-uid, jest 8/8; UI multi-select+manual). **Bonus:** fix bug redirect-loop token-expiry (proxy.ts). Ảnh royal-*.png. Còn: commit; AC4 gửi thật chờ Tuya subscribe. Chốt: bỏ filter iOS/Android + admin tạo-mới seed tay | 2026-07-02 | [↗](m1-admin-royal-redesign/progress.md) |
| m1-mobile-apple-login | M1·B2 | mobile+lib | TEST | **B1–B5 code XONG + verified** (tsc 0 · eslint 0 err · jest 44/44 · plist OK). Sign in with Apple (iOS) → Tuya `loginByAuth2WithType:'ap'`. B1 install `@invertase/react-native-apple-authentication@2.5.1` · B2 `appleAuth.ts`(jest 5/5) · B3 `doThird('ap')`+`auth.ts` extraInfo Apple · **B4 impl iOS native `thirdLogin` (`loginByAuth2WithType:`+JSON→NSDictionary) - bỏ stub, UNBLOCK cả Google iOS** · B5 entitlement+`APPLE_SIGNIN_SETUP.md`. Research: [tuya-ios-third-party-login.md](../docs/research/tuya-ios-third-party-login.md). Còn **B6/AC6 device BLOCKED** (Xcode capability+pod, Apple Dev Program, Mac build). Cần `git add` appleAuth.ts+test+entitlements+SETUP.md | 2026-07-02 | [↗](m1-mobile-apple-login/progress.md) |
| m1-mobile-google-login | M1·B2 | mobile | **DONE (Android)** | **AC6 ANDROID VERIFIED 2026-07-02** (SM-A325F): build thật (JDK17 + `security-algorithm.aar`→`app/libs` + `secrets.properties`; `assembleDebug` 164MB) → Google login E2E: picker→consent→idToken(aud=Web `rh83..`)→`thirdLogin('gg')`→**Home**, persist qua restart; 2 user Tuya thật (`imax.dev.sn@`,`showroom.imax@`). **🐛 FIX bug logout** không gọi `signOutGoogle` → picker không hiện; vá `src/state/useAuth.ts`, verified on-device (picker đủ 3 acc). **🎯 DC (không phải bug code):** account rơi **Western Europe** ≠ project **Central Europe** → root cause `/users` rỗng ([[m1-backend-user-mgmt]]). Verify verbatim: DC do **NGÀY TẠO appKey**, KHÔNG countryCode nào cứu app post-split. Central Europe self-serve bất khả thi (App SDK)→ticket Tuya / Option B. Đã **expose country code** trên AuthScreen (chỉ để xem/thử, không đổi được DC). iOS device chờ Mac | 2026-07-02 | [↗](m1-mobile-google-login/progress.md) |
| m1-mobile-pairing | M1·B4 | mobile | DEV | in_progress - **B1–B4 code XONG** (adapter pairing+mock · deviceStore AsyncStorage · PairingScreen state-machine Wi-Fi/BLE · success→connectDevice). `tsc`/`jest` deferred (no node_modules+503). Còn AC5 device round-trip + thay `ensureHome` tạm bằng auth/home thật | 2026-06-30 | [↗](m1-mobile-pairing/progress.md) |
| m1-mobile-scaffold | M1·B(clone UI) | mobile | DEV | in_progress - **B1–B6 code XONG** (clone UI 12 screens + **B6 nối lib Tuya**: adapter require+mock, DP-id, useAppState device→Tuya). `tsc`/`jest` deferred (no node_modules+503). Còn AC6 device round-trip (build+thiết bị+DP schema) → rồi feature mobile pairing/dashboard | 2026-06-30 | [↗](m1-mobile-scaffold/progress.md) |
| m1-mobile-dashboard | M1·B5 | mobile | DEV | in_progress - **B1–B9 code XONG + verify PASS** (mạng đã thông → `npm install`+`jest` 34/34+`tsc` clean+`eslint` 0 lỗi; sửa 2 divergence code-vs-đĩa phát hiện qua verify thật: `lib/format.ts`+`lib/debounce.ts` từng ghi "code xong" nhưng chưa từng tồn tại trên đĩa + 1 TS-quirk `withTimeout<any>`). **AC7 ĐẠT.** Còn: H-2 DP schema thật + AC6/HW1-8 chờ build+thiết bị; nit L-1/L-2/L-3 backlog | 2026-06-30 | [↗](m1-mobile-dashboard/progress.md) |
| m1-notification-history | M1·B+C | backend+mobile | PLAN | **Lịch sử notification per-user** (inbox trên app, đọc từ backend). Backend lưu `NotificationLog` mọi tin gửi (Tuya+FCM) → app đọc history của mình. Scope user chốt: **chỉ user-app + backend per-user auth**. FCM hiện không có message center → cần log backend mới xem lại được. **Blocker chính = auth mobile→backend per-uid** (không dùng api-key vì là dữ liệu cá nhân) - **nền DÙNG CHUNG với `m1-device-reminders`**, làm 1 lần 2 feature xài. 7 bước (B1 research auth → B5 /me/notifications → B6 mobile). Chờ **Gate ①** | 2026-07-06 | [↗](m1-notification-history/progress.md) |
| m1-push-provider-fcm | M1·C+D | backend+mobile+admin | TEST | **B1–B5 CODE XONG + verify độc lập XANH (2026-07-06):** backend tsc0/jest 45/45 · mobile tsc0/jest 83/83 · admin tsc0; file thật trên đĩa; DI wiring OK; migration `recreate_push_tokens` applied; `.env` đã điền FCM creds thật. **Option gửi push qua FCM** song song Tuya, chọn bằng ENV `NOTIFICATION_PROVIDER=tuya\|fcm` (default tuya). Abstraction `NotificationProvider` (Tuya\|Fcm) + router; `/notifications/send` giữ nguyên UI + shape; admin badge "Sending via". Mobile đăng ký token cả Tuya + backend (`POST /push/tokens`, api-key). AC1–AC5 đạt (code/unit). **Còn B6/AC6 = E2E device - BLOCKED:** cần (a) deploy/run backend có push module + 4 ENV, (b) rebuild Android + login → token vào `push_tokens`, (c) admin gửi → nhận FCM. ⚠️ iOS defer (Mac+APNs); ⚠️ data residency EU (FCM=Google/US) cần client OK trước prod. | 2026-07-06 | [↗](m1-push-provider-fcm/progress.md) |
| m1-device-reminders | M1·B+C | mobile+backend | TEST | **CODE-COMPLETE B1–B8 (2026-07-06):** backend (model+service+countdown+auth api-key/uid/ownership+controller `/reminders/:deviceId`+cron `scanDue→push`) + mobile (client `services/reminders.ts` + `FilterReminderCard` trên Device Detail). Verify **backend jest 67/67 · mobile 90/90 · tsc0/eslint0**. Còn: **apply migration (DB P1001)** + **E2E** (deploy backend + PUSH_API_KEY mobile + rebuild). Auth nền DÙNG CHUNG `m1-notification-history`. - **Nhắc bảo trì theo thiết bị** _(B1–B7 backup)_ backend HOÀN CHỈNH (model+service+countdown+auth api-key/uid/ownership+controller `/reminders/:deviceId`+cron `scanDue→push` vercel `0 1 * * *`) + mobile client `services/reminders.ts`. Verify **backend jest 67/67 · mobile 90/90**. Còn **B8 mobile UI card + apply migration (DB P1001)**. Auth nền DÙNG CHUNG `m1-notification-history`. ~~B1–B5~~ - **BACKEND CRUD** model `DeviceReminder`(unique deviceId)+migration(⚠️apply defer DB P1001) · `RemindersService`+`computeStage` · **auth chốt = api-key+uid+ownership** (`MobileAuthGuard`+`DeviceOwnershipGuard`, note tuya-app-session-verification) · `RemindersController` /reminders/:deviceId. Verify **backend jest 59/59**. Còn **B6 cron push · B7/B8 mobile · apply migration**. Nền auth DÙNG CHUNG `m1-notification-history`. - **Nhắc bảo trì theo thiết bị** (filter reminder). Chuyển từ local+global → **backend source-of-truth + Tuya server push**, mỗi thiết bị **1 reminder** (unique deviceId), countdown 90 ngày + "Replaced now" reset. Blocker chính: **app chưa có kênh gọi backend** (services/api.ts xoá REV 2) → B1 research auth mobile→backend (token verify / fallback ownership `deviceId∈uid` qua Tuya Cloud). Server push chặn bởi **duyệt template** (≤2 ngày). 10 bước (B1 research → B2 model → B6 cron push → B8 UI). Chờ **Gate ①** | 2026-07-06 | [↗](m1-device-reminders/progress.md) |
| m3-into-the-cold | M3 | mobile | TEST | **CODE-COMPLETE B1–B4 (2026-07-06):** (1) `ritualStore` persist + nối useAppState (**fix bug state ritual mất khi restart**); (2) SessionScreen **đếm ngược** (timed preset 1/2/3/5' + auto-finish) + giữ open, logic `lib/sessionTimer`; (3) `DailySummaryScreen` riêng (Today + 7-day bars) + route + entry Progress. Verify **tsc0 · eslint0 · mobile jest 101/101**. Còn: verify UI trên máy + commit. | 2026-07-06 | [↗](m3-into-the-cold/progress.md) |
| m1-admin-push | M1·C+D | backend+admin | TEST | Option A **Tuya Cloud App Push** - code XONG cả 2 đầu + **jest 8/8**: backend gửi **theo user** (`uids[]`/`all`, loop per-uid) + admin UI multi-select + template CRUD; mobile đầu-nhận = NotificationsScreen đọc Message Center ([↗ home-device-flow B14]). **CONSOLE ĐÃ CẤU HÌNH (user, 2026-07-03): subscribe App Push + upload cert FCM(Android)+APNs(iOS)**. **BE FREE-FORM XONG (2026-07-03):** `POST /notifications/send` (title+body → template `${title}/${content}`, đọc `TUYA_APP_TEMPLATE_ID`+`TUYA_APP_BIZ_TYPE` từ env) + admin form đổi sang gọi endpoint này (bỏ FCM `/push/send`); jest 12/12. Còn: (1) điền `TUYA_APP_BIZ_TYPE`+`TUYA_APP_TEMPLATE_ID` vào `.env`; (2) template có biến `${title}/${content}` đã duyệt; (3) gửi thật E2E (admin→app); (4) app nhận (registerDevice) = M3 | 2026-07-03 | [↗](m1-admin-push/progress.md) |

## Backlog M1 - chia theo brief mở rộng (chạy `/plan <slug>` khi tới)
> Thứ tự phụ thuộc: **A → B**; **C → D** (track backend/admin chạy song song được).
| Feature (slug) | Milestone | Phần | Mô tả ngắn |
|---|---|---|---|
| m1-mobile-home-setup | M1·B3 | mobile | Setup Home đầu tiên cho user (1 nhà/user) |

## Backlog M2 / M3 (ngoài phạm vi M1)
| Feature (slug) | Milestone | Phần | Mô tả ngắn |
|---|---|---|---|
| m2-ui-completion | M2 | mobile | Hoàn thiện UI từ design (đèn, UV, defrost), tối ưu mobile |
| m2-backend-device-mapping | M2 | backend | User profile chi tiết + device mapping + RLS (phần backend còn lại) |
| m2-home-rooms-roles | M2 | mobile | Quản lý nhà nâng cao: gán phòng, phân quyền thiết bị, multi-home |
| m2-admin-device-usage | M2 | admin | Admin web: theo dõi trạng thái thiết bị + dữ liệu usage (phần admin còn lại) |
| m3-into-the-cold | M3 | mobile | Đồng hồ đếm ngược + daily summary |
| m3-push-fcm | M3 | mobile+backend | Push notification qua Firebase (nhắc dọn, cảnh báo) |
| m3-filter-reminder | M3 | mobile | Nhắc bộ lọc: trạng thái + reset + link mua |
| m3-testing-delivery | M3 | all | Kiểm thử toàn diện, tối ưu, bàn giao mã nguồn + tài liệu |

## Đã xong
| Feature (slug) | Milestone | Phần | Ngày xong | Đã audit? | Thư mục |
|---|---|---|---|---|---|
| - | | | | | |

## Đã thay thế (Superseded)
| Feature (slug) | Thay bằng | Ngày | Lý do |
|---|---|---|---|
| m1-scaffold-and-tuya-init | m1-tuya-sdk-library + m1-mobile-scaffold | 2026-06-28 | Đổi hướng: wrap Tuya SDK thành thư viện npm riêng (TurboModule) thay vì nhúng native vào app; M1 mở rộng scope |

## Bị chặn
| Feature (slug) | Đang chờ gì | Từ ngày |
|---|---|---|
| m1-tuya-sdk-library | Build-ready đã wire (2026-06-29). **JDK17 + Android SDK xác nhận đã có sẵn trên máy (2026-06-30)** - không còn là blocker. Còn: console applicationId TRÙNG package + keystore SHA-256 + DC. iOS cần Mac. Xem progress.md. | 2026-06-29 |

---
### Quy ước
- **Phase:** PLAN → DEV → TEST → FIX-PLAN → DONE (xem `.claude/skills/dev-loop/SKILL.md`).
- **Milestone:** M1 Nền tảng, Kết nối lõi & Quản lý User (mở rộng - ngân sách cần ET lại) · M2 UI & Backend/Admin nâng cao · M3 Nâng cao & bàn giao.
- **Phần:** mobile (RN CLI) · lib (`packages/tuya-react-native`) · backend (NestJS+Supabase) · admin (Next.js web).
