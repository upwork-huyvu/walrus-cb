# Progress: Radar auto-discovery cho màn pairing (giống Smart Life)

> File quản lý tiến trình (state machine của feature). `/dev`, `/test`, `/fix-plan`
> đọc đầu vào và cập nhật cuối mỗi lượt. Luôn giữ phần "Hành động kế tiếp" chính xác.

- **Slug:** `m1-pairing-radar-discovery`
- **Phase hiện tại:** `TEST`
- **Trạng thái:** `in_progress`
- **Cập nhật lần cuối:** 2026-07-16 03:00

## ▶ Hành động kế tiếp (đọc cái này trước tiên)

**revise #1 ĐÃ HIỆN THỰC XONG (B7+B8+B9).** Gate ② client duyệt ("có làm luôn"). Tự verify tại
chỗ: tsc **0** · eslint **0 lỗi** · jest **261/261**. ⇒ **Cài lại APK là test được** (chỉ JS).

Kế tiếp: **B6 - verify trên SM-A325F thật** (chạy `/test` để sinh checklist). Ưu tiên:
1. **AC1** - chọn mode **BLE** → app **phải hỏi quyền Bluetooth**. Đây là blocker gốc; không thấy
   prompt thì mọi thứ còn lại vô nghĩa. (EZ/AP **không** xin quyền BLE - đúng thiết kế.)
2. **AC8** - EZ: dropdown Network name có quét ra mạng không, chọn xong có thu lại không.
3. **AC7/AC2** - mode BLE: sensor combo có hiện blip trên radar không.
4. **AC9** - AP: 2 ô phải TRỐNG khi vào (không prefill) - đây là chỗ dễ hồi quy nhất.
5. 🔴 **Ca iOS/AP: token lấy khi đã mất mạng** (xem Blockers) - cần máy iOS.

Song song (không chặn): client làm **test 30 giây** - mở Smart Life vào màn radar "Searching for
nearby devices…", **không** bấm "Add manually", xem sensor có tự hiện lên không. Hiện → sensor có
BLE beacon → AC7 chạy được. Không hiện → sensor Wi-Fi thuần → blip chỉ đến từ kênh EZ (plan mục 5).

## Checklist các bước (đồng bộ với plan.md mục 4)

- [x] B1 - Permission gate BLE (`blePermissions.ts`) · **done** · 9 test · ✅ revise #1 **không đụng**
- [x] B2 - Radar model (`radarModel.ts`, logic thuần) · **done** · 23 test · ✅ revise #1 **không đụng**
- [x] B3 - Registry mode (`pairingModes.ts`) · **done** · 23 test · ⚠️ **B7 viết lại** (bỏ `auto`)
- [x] B4 - Component `RadarView.tsx` (SVG + blip chạm được) · **done** · 7 test · ✅ revise #1 **không đụng**
- [x] B5 - Ráp vào `PairingScreen.tsx` · **done** · ⚠️ **B8/B9 sửa lại** (bỏ nhánh song song, Wi-Fi theo mode)
- [x] B7 - `pairingModes.ts` viết lại: 3 mode, `channel` (số ít), `steps[]`, `wifiInput` · **done** · 26 test
- [x] B8 - Ô nhập Wi-Fi theo mode: dropdown (EZ) vs tự nhập (AP) vs ẩn (BLE) · **done**
- [x] B9 - `PairingScreen`: bỏ nhánh song song + render hướng dẫn đánh số · **done**
- [ ] B6 - Verify thiết bị thật SM-A325F · pending · **kế tiếp (chạy CUỐI)** · cần người + máy

## Checklist tiêu chí hoàn thành (đồng bộ với plan.md mục 3)

- [ ] AC1 - Android 12+ hỏi quyền BLE; từ chối → lý do rõ + nút Settings, không quay mù
- [ ] AC2 - Radar hiện 1 blip/thiết bị, nhiều blip cùng lúc, vị trí ổn định giữa re-render
- [ ] AC3 - Chạm blip → popup xác nhận → Add → connecting → pair thật
- [ ] AC4 *(viết lại)* - Dropdown **3 mode** Android (EZ·AP·BLE) / **2** iOS (AP·BLE), **không có auto**; default Android=EZ, iOS=AP
- [ ] AC5 *(viết lại)* - **Mỗi mode đúng 1 kênh**, không chạy song song
- [ ] AC6 - `tsc` 0 · `eslint` 0 · `jest` xanh + test mới (blePermissions · radarModel · pairingModes)
- [ ] AC8 *(mới)* - EZ: Network name là **dropdown có scan**, chọn xong **collapse**; không quét được → prefill mạng đang nối
- [ ] AC9 *(sửa 2026-07-16)* - AP: **KHÔNG tự điền** mạng đang nối (prefill = sai vì có thể đang ở hotspot thiết bị) - nhưng **Android vẫn có dropdown quét** (quét = mọi mạng xung quanh ⇒ an toàn); iOS gõ tay. Cảnh báo "Wi-Fi nhà, không phải SmartLife" hiện ở cả 2 dạng
- [ ] AC10 *(mới)* - Mỗi mode có **hướng dẫn đánh số** chi tiết; AP nêu rõ bước nối Wi-Fi thiết bị; AP iOS ≠ AP Android
- [ ] AC7 - SM-A325F: sensor combo hiện blip + pair thành công · **cần thiết bị thật**

## Nhật ký chạy (Run log) - mới nhất ở trên

> Mỗi lần DEV/TEST/FIX-PLAN ghi 1 dòng: thời gian · bước · kết quả · ghi chú.

| Thời gian | Phase/Bước | Kết quả | Ghi chú / output |
|---|---|---|---|
| 2026-07-16 04:10 | DEV (AP iOS: getDeviceSecurityConfigs + regInfo) | ✅ | Client yêu cầu thêm cấu hình AP iOS. **3/4 phần ĐÃ CÓ SẴN** → không làm lại: Info.plist đã có `NSLocalNetworkUsageDescription` + `NSLocationWhenInUseUsageDescription` (dòng 49/51, tiếng Anh - khớp UI app); entitlement `com.apple.developer.networking.wifi-info` đã có, **KHÔNG** có multicast (đúng - đó là của EZ); `CLLocationManager.requestWhenInUseAuthorization` + `NEHotspotNetwork.fetchCurrent` **đã do `react-native-wifi-reborn` làm** (`RNWifi.m:37,263`) → tự viết là nhân đôi. **Từ chối viết bản Swift song song:** `ThingSmartActivator` là **singleton, `delegate` là 1 property duy nhất** (`TuyaPairing.mm:126`) ⇒ 2 implementation cùng set delegate = **cái sau đè cái trước, cái kia chết im lặng**. **LỖ THẬT client bắt đúng:** AP legacy thiếu `getDeviceSecurityConfigs` → verify **header SDK thật** (`Pods/ThingSmartActivatorCoreKit/.../ThingSmartActivator.h:130-138`): luồng AP legacy là **3 bước** token → securityConfigs → `startConfigWiFi:...regInfo:`; code cũ bỏ bước 2, gọi biến thể không regInfo → **đã sửa trong `TuyaPairing.mm`** (regInfo nullable ⇒ lookup hỏng vẫn pair tiếp; EZ giữ nguyên). 🐛 **Suýt tự tạo bug:** step mới ban đầu đặt `..._failed` → chứa "fail" → `isFailureStep()` xếp nhầm thành lỗi → đổi `sdk.security_configs_skipped` + chi tiết vào `dataJson`, không set error*; kèm `pairing.ts` giờ **log cả dataJson**. ⚠️ **ĐỤNG NATIVE ⇒ phải rebuild iOS** (phá cam kết "chỉ JS" của feature) + **CHƯA compile được** (chưa chạy pod install/xcodebuild). Verify JS: tsc **0** · jest **267/267**. |
| 2026-07-16 03:40 | DEV (dropdown cho AP/Android + mũi tên back) | ✅ | Mũi tên back **20→26** (nằm cạnh title 28px nên 20 lọt thỏm; 8 màn khác giữ 20 - đã comment lý do tại chỗ). **Client chỉ ra chỗ tôi gộp nhầm khái niệm:** *"chỗ chọn network name trong android nếu scan được thì cũng để dropdown"* - đúng. **`wifiInput` và `prefillCurrentWifi` là HAI thứ khác nhau, tôi gộp làm một rồi cấm cả hai ở AP:** `scanWifiNetworks()` trả **mọi mạng xung quanh**, không liên quan đang nối mạng nào ⇒ **luôn an toàn** (và ở màn nhập thì máy **vẫn còn ở Wi-Fi nhà** - bước nối hotspot đứng SAU bước nhập); chỉ `getCurrentWifiSsid()` (**mạng đang nối**) mới nguy hiểm ở AP. ⇒ Tách: `wifiInput` = "quét được hay gõ tay" (**Android dropdown cho cả EZ lẫn AP**, iOS manual); `prefillCurrentWifi` = "được tự điền không" (EZ true · AP false, gate cả nút "Use the network I'm connected to"). Thêm `wifiNotice` vào spec để cảnh báo AP hiện ở **cả** dropdown lẫn manual (trước hard-code trong `wifiManualCard` ⇒ bật dropdown là mất cảnh báo). Verify: tsc **0** · eslint **0** · jest **267/267** (+5). |
| 2026-07-16 03:25 | DEV (UI đầu màn + đính chính AP) | ✅ | Client góp ý UI: (1) **back cùng hàng title** (bỏ back đứng riêng phía trên; màn `error` giữ back riêng vì không có title), (2) title **"Pair your Walrus" 1 dòng** (`numberOfLines={1}`+`adjustsFontSizeToFit` lo máy hẹp; bỏ nhãn "DEVICE PAIRING"), (3) **bỏ khối "Same Wi-Fi required"**. (4) ❌ **ĐÍNH CHÍNH quan trọng - client bắt đúng lỗi của tôi:** hướng dẫn AP/Android cũ bảo *"khỏi rời app, SDK tự nối hotspot"*, dựa vào **1 dòng** doc Android. Dòng đó **mâu thuẫn với SmartLife user manual** (manual mô tả app CHẠY THẬT, bắt user tự vào Wi-Fi settings chọn `SmartLife…`, không phân biệt nền tảng) **và nhiều khả năng lỗi thời**: từ **Android 10** app không tự join Wi-Fi ngầm được nữa (phải qua `WifiNetworkSpecifier` → hệ thống hiện dialog), mà app này targetSdk **36**. ⇒ **Cả 2 nền tảng** giờ đều hướng dẫn user **tự nối hotspot `SmartLife…`** + nhắc **giữ máy ở hotspot tới khi xong**. An toàn 2 chiều: SDK có tự nối thật → user thấy đã nối sẵn, không hại; không tự nối → hướng dẫn cũ sẽ khiến pair fail mà không ai hiểu vì sao. 2 test cũ (khẳng định Android KHÔNG ra Settings) **đã đảo lại** + thêm test hồi quy chặn chuỗi "no need to leave the app". Đã ghi mục **ĐÍNH CHÍNH** vào research note. 🔴 **B6 phải xác nhận trên máy thật: chạy AP mà không tự nối hotspot - SDK có tự nối không?** Verify: tsc **0** · eslint **0** · jest **262/262**. |
| 2026-07-16 03:00 | DEV B7+B8+B9 | ✅ | Gate ② duyệt ("có làm luôn"); iOS default = **AP** đúng thứ tự client chỉ định. **B7** `pairingModes.ts` viết lại (26 test): 3 mode, **`channel` số ít** (kiểu dữ liệu chặn việc gộp kênh trở lại), `steps: string[]`, `wifiInput`; `defaultPairingMode(platform)` = phần tử đầu. **B8** tách `wifiCard()` → `wifiDropdownCard()` (EZ: bấm là xổ + **tự quét luôn**, không bắt bấm thêm nút Scan; chọn → **collapse**; vẫn có ô gõ tay cho mạng ẩn) và `wifiManualCard()` (AP: 2 ô trống + banner vàng "đây là Wi-Fi NHÀ, không phải hotspot SmartLife"); **chặn prefill khi `wifiInput==='manual'`** + **xoá ssid/password khi đổi mode** (EZ→AP mà giữ nguyên thì AP thừa hưởng đúng cái prefill nó vừa cấm). **B9** `startScan()` rút theo `mode.channel` - **xoá `wifiChannelOf()` + nhánh "kênh Wi-Fi hỏng nhưng BLE vẫn quét"**; lỗi Wi-Fi giờ `finishError` thẳng; `stepsCard()` render hướng dẫn **đánh số**; màn radar chỉ nhắc **bước 1** (không dội cả danh sách). Sửa nhãn "recommended": chỉ gắn **iOS/AP** (có nguồn Tuya) - Android EZ **không** gắn vì doc nói EZ tỉ lệ thành công **thấp hơn** AP, gắn vào là tự bịa. Verify: tsc **0** · eslint **0 lỗi** · jest **261/261**. |
| 2026-07-16 02:10 | FIX-PLAN (revise #1) | ⏸️→✅ | **Client review B1-B5 → đổi yêu cầu** (không phải plan sai kỹ thuật, cũng không phải code lỗi: `auto` là **suy luận thiết kế của tôi** để mô phỏng UX "không hỏi mode" của Smart Life; client xem bản chạy rồi chọn hướng khác). **4 thay đổi:** (1) **bỏ `auto`**, còn 3 mode - Android EZ·AP·BLE, iOS AP·BLE, default Android=EZ / iOS=AP; (2) EZ: Network name thành **dropdown có scan bên trong**, chọn xong **collapse**; (3) AP: ssid+password **tự nhập, cấm prefill**; (4) cần **hướng dẫn từng bước đánh số** cho từng mode. **Hệ quả kiến trúc:** mỗi mode giờ **đúng 1 kênh** ⇒ **thiết kế EZ‖BLE song song bị xoá** ⇒ **rủi ro số 1 của plan biến mất** (không doc nào bảo đảm 2 activator chạy song song an toàn) + `startScan()` mất nhánh khó test nhất. **Mất:** iOS không còn "vào màn là radar quay luôn" (default AP cần Wi-Fi trước) - client đã chốt, không phải bug. **Điểm client đúng và khớp doc:** cấm prefill ở AP - lúc đó máy đang nối hotspot thiết bị nên "Wi-Fi đang kết nối" = `SmartLife-xxxx`, mà SDK cần SSID **của router**. Plan: AC4/AC5 viết lại, thêm AC8/AC9/AC10, thêm B7/B8/B9, B6 lùi xuống cuối. B1/B2/B4 **không đụng**. **Chờ gate ②.** |
| 2026-07-16 01:30 | DEV B4+B5 | ✅ | **B4** `RadarView.tsx` (SVG: vòng đồng tâm + tia quét 60° xoay + blip `Pressable`). 🐛 **2 bug bắt được lúc test:** (1) `Animated.loop` vô hạn + `act()` → jest **TREO IM** (không fail, không log) → phải `jest.useFakeTimers()`; đây là test component **đầu tiên** của repo nên chưa ai gặp. (2) `findAllByType(Pressable)` trả **RỖNG** → test "blip nằm trong khung" **PASS RỖNG** (vòng lặp không chạy lần nào) → đổi sang lọc theo testID. **B5** ráp `PairingScreen`: `startScan()` chạy **mọi kênh của mode SONG SONG**, xin quyền BLE **trước** khi quét, thiếu quyền → banner đỏ + nút Settings/Try again thay vì quay mù; `onBleScan`/`onPairingProgress` → `upsertBlip` (bỏ hẳn nhảy thẳng sang 'found' ở thiết bị đầu tiên); chạm blip → **Modal popup** 3 nhánh (pair được / blip EZ "đang tự thêm, khỏi làm gì" / combo thiếu Wi-Fi → về intro); dropdown 4 mode thay cặp nút EZ/AP; **xoá** step `prepare` + `found`. **Dọn code chết:** `pairingOrb('searching')` + `radarWave1/2/3` không còn ai gọi → gộp còn `connectingOrb()`. **Sửa 2 lỗi tự phát hiện:** blip cũ còn sót khi quét hỏng vì thiếu quyền (dọn trước mọi nhánh return); kênh Wi-Fi hỏng **không** được giết cả màn khi BLE vẫn quét song song. Verify: tsc **0** · eslint **0 lỗi** (135 warning inline-style là convention sẵn có của file) · jest **258/258** (+7). |
| 2026-07-16 00:45 | RESEARCH + DEV B3 (revise) | ✅ | Client chốt "**iOS để AP, EZ chưa support**" + gửi doc AP iOS → chạy `/tuya-research` → note mới [tuya-ios-ap-mode-pairing.md](../../docs/research/tuya-ios-ap-mode-pairing.md) (6 trang doc chính chủ, có trích dẫn). **Phát hiện lớn: AP legacy KHÔNG cần entitlement Apple** - "Hotspot capability" + `ThingSmartHotspotCredentialKit` **chỉ thuộc mục "New AP pairing process" (= AP Plus)**; fetch riêng để xác minh vị trí 2 dòng đó ⇒ chọn AP trên iOS là **thoát thật**. Tuya cũng nói thẳng *"we recommend that you use the AP mode instead of the Wi-Fi EZ mode"* (iOS 14.5+) ⇒ quyết định của client khớp doc. **Sửa `pairingModes.ts`:** iOS bỏ EZ, iOS `auto`=BLE thuần (`needsWifi:false` ⇒ **vào màn là radar luôn**, đúng ảnh client - ảnh chụp iPhone), dropdown iOS còn `auto`+`ap`; hướng dẫn AP **tách theo nền tảng** (Android SDK **tự nối hotspot**, iOS bắt user ra Settings). **Sửa lỗi nội dung của chính mình:** bản đầu thiếu bước **đèn nháy CHẬM** (AP) - viết theo trí nhớ, không nguồn. Verify: tsc **0** · eslint **0** · jest **251/251** (+7). 🔴 **Rủi ro mới, chưa gỡ:** iOS/AP có thể lấy token **khi đã mất mạng** (`startWifiPairingAuto` tự lấy token ngay trước khi chạy, mà lúc đó điện thoại đang ở hotspot thiết bị) - xem context.md. |
| 2026-07-16 00:20 | DEV B1+B2+B3 | ✅ | Gate ① client duyệt ("ok, bắt đầu làm đi"). 3 file service + 3 file test, **chưa nối vào UI** (B5 mới nối). **B1** `blePermissions.ts`: `ensureBlePermissions()` trả kết quả có kiểu (`denied`/`blocked`/`unsupported`), API 31+ xin SCAN+CONNECT+**LOCATION** (manifest thiếu `neverForLocation` ⇒ location bắt buộc), API ≤30 chỉ LOCATION, iOS no-op. `never_ask_again` → message chỉ sang Settings. **B2** `radarModel.ts`: blip từ 2 nguồn, dedupe theo key, **toạ độ tất định qua hash FNV-1a** (AC2 - random thì blip nhảy loạn mỗi event scan, không chạm trúng), giữ thứ tự first-seen, cap 8, `isPairableBlip` chỉ true với BLE. **B3** `pairingModes.ts`: 4 mode đúng thứ tự client (auto→ez→ap→ble), default `auto`, `auto.channels=['ez','ble']` **không có AP** (test khoá cứng ràng buộc này). Verify tại chỗ: `tsc` **0** · `eslint` **0 lỗi** (3 file mới sạch cả warning; 5 warning còn lại là của test cũ) · `jest` **244/244** (+48 mới, trước là 196). |
| 2026-07-15 23:55 | PLAN | ✅ | Tạo plan 6 bước + 7 AC. Chốt 3 ràng buộc vật lý với client: EZ không discovery được (radar phải sau bước Wi-Fi) · EZ‖AP bất khả (⇒ Auto = EZ‖BLE, AP chỉ qua dropdown) · blip EZ không gate được bằng tap. Phát hiện blocker: Android chưa xin `BLUETOOTH_SCAN` runtime (manifest có khai, code không request) → `startLeScan` im lặng. Chốt scope: chỉ JS, không wire unified `ActivatorService` (nợ kỹ thuật có chủ đích). |

## Vấn đề đang chặn (Blockers)

- ~~⏸️ Gate ①: chờ client duyệt plan~~ → **đã duyệt 2026-07-16.**
- ❓ **Chưa xác nhận sensor của client có BLE beacon hay không** → quyết định AC7 và độ hữu ích
  của cả feature. Gỡ bằng test 30 giây ở mục "Hành động kế tiếp" (không chặn B1-B5).
